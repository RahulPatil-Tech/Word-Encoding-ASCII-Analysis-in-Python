# Word-Encoding-ASCII-Analysis-in-Python
This project demonstrates basic text analysis and encoding concepts using Python.   It converts characters into binary (ASCII), performs numerical analysis, and analyzes character frequency.
